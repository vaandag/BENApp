# BENApp V171 — Comment Isolation + V168 Plus Baseline

- V168-based project tree used as the baseline available in the workspace.
- The existing central + navigation item/flow is preserved; no redesign is applied here.
- Comments are persisted by immutable memory ID when available.
- API comments are filtered by memory_id/memoryId when the field is supplied.
- Local/server comment de-duplication also considers creation time.
- Existing BEN master logo asset/component is retained.
