# BEN V46 — User System

- Real PHP/SQLite registration and login endpoints.
- Passwords stored with PHP `password_hash` / verified with `password_verify`.
- Session token is returned to the Flutter client for the current app session.
- User lookup/search and profile summary endpoints added.
- Follow endpoint now toggles follow/unfollow.
- Flutter account screen now calls the real API instead of demo-only messages.
- Existing demo path remains available.
- Version: 4.4.0+46.
