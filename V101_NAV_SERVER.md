# V101 NAV + SERVER
- Bottom navigation restored to BEN / Harita / Kesfet / Mesajlar / Profil.
- CANLI is opened from the video button in the home header.
- Global memory + button is lifted above the bottom navigation and no longer blocks Kesfet.
- Removed redundant visible plus icons from story self and empty feed actions.
- Fixed Live API paths: ApiClient already targets `/api`, so calls now use `live` instead of `/api/live`.
- Added Windows local PHP server scripts/docs.
